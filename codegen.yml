overwrite: true
schema: "http://localhost:8080/graphql"
documents: "network/graphQl/**/*.graphql"
generates:
  network/graphQl/interfaces/types.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
    config:
      withHooks: true
      
